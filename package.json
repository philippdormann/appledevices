{
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "release": "bumpp package.json packages/*/package.json --all",
    "ci:publish": "zx scripts/publish.js",
    "build": "node build.js",
    "dev": "pnpm -C packages/example run dev"
  },
  "type": "module",
  "devDependencies": {
    "bumpp": "^9.0.0",
    "got": "^14.4.2",
    "zx": "^8.1.8"
  }
}
